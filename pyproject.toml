[project]
name = "ai-ecosystem"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/*",
    "packages/*"
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"