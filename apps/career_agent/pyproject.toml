[project]
name = "career-agent"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
    "shared-utils",
    "openai>=2.29.0",
]

[tool.uv.sources]
shared-utils = { workspace = true }